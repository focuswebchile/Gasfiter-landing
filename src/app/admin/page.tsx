import StagingWorkflowPanel from "@/components/staging-workflow-panel";

export default function AdminPage() {
  return (
    <div style={{ minHeight: "100vh", width: "100%", background: "#f4f6fa" }}>
      <StagingWorkflowPanel />
    </div>
  );
}
